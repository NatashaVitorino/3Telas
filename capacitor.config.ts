import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: '3TelasOficial',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
